[newrelic]
# --------------------------------------------------------------------------
# Clave de licencia — se sobreescribe con la variable de entorno
# NEW_RELIC_LICENSE_KEY si está definida (recomendado en producción).
# --------------------------------------------------------------------------
license_key = %(NEW_RELIC_LICENSE_KEY)s

# Nombre de la aplicación tal como aparece en el dashboard de New Relic.
# Se puede sobreescribir con NEW_RELIC_APP_NAME.
app_name = %(NEW_RELIC_APP_NAME)s

# Activar/desactivar el agente sin cambiar código.
monitor_mode = true

# Nivel de log del agente (critical, error, warning, info, debug, verbosedebug).
log_level = info

# Ruta del log del agente. En contenedores se recomienda stdout.
log_file = stdout

# ------------------------------------------------------------------
# Apdex T (segundos) — umbral para clasificar transacciones como:
#   Satisfied  : tiempo ≤ apdex_t
#   Tolerating : tiempo > apdex_t y ≤ 4 * apdex_t
#   Frustrated : tiempo > 4 * apdex_t
# Un valor de 0.5 significa: ≤500ms satisfecho, ≤2s tolerable, >2s frustrado.
# ------------------------------------------------------------------
apdex_t = 0.5

# Instrumentación automática de Flask y SQLAlchemy está activa por defecto.
# No se requiere configuración adicional para esas librerías.

[newrelic:development]
monitor_mode = false

[newrelic:test]
monitor_mode = false
